<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 扫码接收配置（二维码传送）需要相机；无相机设备不影响其他功能 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <!-- Android 11+ 包可见性：声明要查询所有可启动应用 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <!-- 微信包可见性声明，确保 resolveActivity() 在 Android 11+ 上能正常工作 -->
        <package android:name="com.tencent.mm" />
        <!-- 支付宝包可见性声明 -->
        <package android:name="com.eg.android.AlipayGphone" />
    </queries>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme"
        android:supportsRtl="true">

        <!-- 主界面 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 设置界面 -->
        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:label="设置"
            android:theme="@style/AppTheme" />

        <!-- 已隐藏应用管理 -->
        <activity
            android:name=".HiddenAppsActivity"
            android:exported="false"
            android:label="已隐藏的应用"
            android:theme="@style/AppTheme" />

        <!-- 数字键绑定管理 -->
        <activity
            android:name=".KeyBindingActivity"
            android:exported="false"
            android:label="数字键绑定应用"
            android:theme="@style/AppTheme" />

        <!-- 二维码配置传送（发送/接收） -->
        <activity
            android:name=".QrTransferActivity"
            android:exported="false"
            android:label="扫码传送配置"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme" />

        <!-- 图片裁剪界面 -->
        <activity
            android:name=".ImageCropActivity"
            android:exported="false"
            android:label="裁剪图片"
            android:theme="@style/AppTheme" />

        <!-- 广告跳过无障碍服务 -->
        <service
            android:name=".service.AdSkipAccessibilityService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    </application>
</manifest>
